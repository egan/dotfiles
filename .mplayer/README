MPlayer Configuration
=====================
This config specifies the default output methods for mplayer, the location of hardware
devices, and the TUI verbosity.

To install, mkfifo .mplayer/pipe and change the string "USER" in .mplayer/config to
reflect you.
